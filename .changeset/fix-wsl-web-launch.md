---
"@bhjia-phys/hakimi": patch
---

web: Fix `/web` under WSL so the authenticated page opens in the default Windows browser, reconnects reused tabs without a hard refresh, and continues receiving live replies.
