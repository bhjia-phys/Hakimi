export default {
  /** Standalone card appended to an assistant reply that successfully
      wrote/edited a previewable file or produced image/video media. */
  cardReady: 'Preview ready',
  cardOpen: 'Open preview',
  /** aria-label of the artifact card's Open button (visible label: `cardOpen`). */
  open: 'Open preview: {name}',
} as const;