---
"@bhjia-phys/hakimi": patch
"@bhjia-phys/hakimi-sdk": minor
---

Add explicit Research Line-to-AITP workstream binding across the Research Manager, Board, and SDK. Bindings carry a server-owned confirmation identity, clear uses exact identity plus a non-rewinding public revision, and scoped checkpoint adoption fails closed when the canonical Entry is not in exactly the captured Topic and single workstream.
