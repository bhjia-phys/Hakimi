# AITP 状态跟踪与交接清单

2026-09-05 Question synthesis 补充：真实临时 H2O 验收暴露 "Entry 已提交但
Question 仍为测试前总结"。Hakimi 只调整现有工具收尾指引：先完成 captured
checkpoint，再为仍匹配当前 Line/Question/revision/cursor 的首次 commit 提示
条件性综合；duplicate 或上下文变化不重复提示。科学评价和证据选择仍由 main
agent 决定，不增加状态、AITP I/O、公开 schema 或科学自动接受。AITP HEAD
`eae1bce5…` 的既有 20 条 dirty paths 保留，working tree 0.9.0/contract 0.2
与官方 CLI 重新核对；对方 dirty handoff 不改，不声称新的 AITP 能力。

## 状态跟踪表

| 核对日期 | AITP HEAD | active stage | 备注 |
|---|---|---|---|
| 2026-08-08 | `8658f6827288f4bb61e5c193a346f0f73ebbe3b2` | M0.6（in progress） | M0/M0.5 done；M1a–M4 blocked。AITP 侧已拍板：prepare/save 按 version-0 契约（不版本化），`enter-0.2` 是第一个版本化契约点；**H0 无 AITP 前置阻塞，可立即实施** |
| 2026-08-14 | `9f9e873440b8d88bfbb2963d8b5717c83b9ef4cc` | M1c（done；deterministic gate passed） | 工作树 clean（除刻意未跟踪 `ref/`、`uv.lock`）。M0.6 以缩小声明关闭；M1a、M1b-R1、M1c 全部 done 且 deterministic gate passed（107 tests）。`list`/`show`/`check` 已 shipped 可 feature-detect；M1c 增加单次 `--workstream` 作用域投影（`enter-0.3`/`list-0.2`）与 repeatable prepare flag。`lineage` 仍 deferred；M2–M4 仍 blocked |
| 2026-08-21 | `2425845063b56852a04abb2fd2f8273b2f30d014` | M1e（done；deterministic gate passed） | committed HEAD/base 是 0.7.0；working tree 有 0.8 Skill-only 草稿（`distilling-methods/SKILL.md`），已把发布面 bump 到 0.8.0（未 commit——无 release commit，0.8.0 不是正式发布版本）。M1a、M1b-R1、M1c、M1d、M1e 全部 done 且 deterministic gate passed（154 tests，committed HEAD 0.7.0）。M1d 增加 scoped `check`（`check-report-0.2`，仅单次 `--workstream`）；M1e 增加 `backfill` 命令（`backfill-0.1`）、`sha256-once:` pin 与 check-policy.json。AITP 0.8 Skill-only amendment（未 release commit）定义 `method-observation` marker、两步 human decision、platform tool/card/Skill 三层——不改 CLI/schema。Hakimi native adapter（H0–H5）未实现；H6/C6 native distillation orchestration planned，unavailable。`lineage` 仍 deferred；M2–M4 仍 blocked |
| 2026-08-23 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（done；deterministic gate passed） | committed HEAD 是 0.8.0——Skill-only amendment 已 commit（`method-observation` marker、两步 human decision、platform tool/card/Skill 三层）。M1a、M1b-R1、M1c、M1d、M1e 全部 done 且 deterministic gate passed（154 tests）。**Hakimi adapter 的 H0–H4 已实现，H5 仅部分集成**，受 flag `KIMI_CODE_EXPERIMENTAL_AITP_RESEARCH_MODE`（默认关闭）门控：strict contract discovery、Python probe、`enter`/`list`/`show`/`check` 读侧消费、`record`/`note prepare|save` 写入门控、scoped `--workstream`、M1e check finding code opaque projection（不实现 backfill/sha256-once/check-policy 语义）、Research state（Question/Line/Focus、三轴问题模型、revision steering、pending checkpoint barrier、Goal complete guard）、mode/loop/Question/Focus/checkpoint 的单一完整 snapshot push、active step 的语义状态维护 guidance、protocol/node-sdk/kap-server/klient 表面、TUI `/research` Board/manager 与 stale-hydrate 防护。`/research on` 只激活 capability 和 Board，不调度模型 turn；Goal 仍是跨 turn continuation 的唯一 owner。设置 flag 开启后才开放 `/research` 与 `EnterAITPMode` 入口，进入模式仍需 `/research on` 或模型入口，inactive 零 AITP I/O。flag off 时（`=0` 或 `/experiments`）所有 AITP 工具、skill 和 Board 隐藏，零 AITP I/O。不自动 init/adopt/inventory/backfill apply；backfill 不作为模型工具暴露。typed question/line registry、literature/compute/Portfolio、H6 未实现。`lineage` 仍 deferred；M2–M4 仍 blocked |
| 2026-08-24 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（done；Hakimi adapter contract correction） | 逐命令复核 managed 0.8 `aitp.contract.json` 与实际 `--help`，未修改 AITP 仓库。Hakimi adapter 修正为严格消费 `enter-0.2/0.3`、`list-0.1/0.2`、`show-0.1`、`check-report-0.1/0.2` 和 version-0 prepare/save envelope；`check` exit 1 作为 findings 报告而非工具失败，stdout exit-2 错误保留 `code/message`，argparse misuse 不再误称整个 adapter degraded；record/Note argv、合法 Entry kind 与 checkpoint error/warning 语义与官方契约对齐。Research guidance 同时禁止用直接 Markdown 读取模拟失败的 `show`，并明确 Research Line 与 AITP workstream 不做静默别名映射；合法 findings（包括 error）不会把 current-state receipt 错报为 degraded。 |
| 2026-08-25 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（done；Hakimi current-state maintenance） | 未修改 AITP 仓库。Hakimi 在 adapter probe ready 后的 mode entry、active undo/cold restore 后，以及 active、admitted 的 Goal continuation turn 在 Research state 发生变化后的 turn end，只读执行 `enter` → `check`；这不是 session-end automatic closeout。maintenance receipt 和 context injection 只暴露安全摘要，完整 Research snapshot/API 或 expanded Board 仍可能包含 checkpoint、revision 和 adapter health 字段。合法的 check findings（包括 error finding）保持 ready；只有周期不可用或无效时显示 degraded。error finding 仍可按具体 checkpoint 的保存屏障阻止提交。不会自动 init/adopt/backfill，也不会自动写 semantic handoff、Entry 或 Note；该 coordinator 不实现 H6 native method-distillation。degraded active Research Mode 会阻止 AITP writes 和 Goal completion，未解决 human gate 也会阻止 Goal completion，但本地 Question/Line mutation 仍可能发生。 |
| 2026-08-27 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（done；Hakimi barrier/alert hardening） | 未修改 AITP 仓库。Hakimi checkpoint 现在保留具体 Entry ID、prepare/save receipt、pre-save baseline 与 post-save check；canonical `record prepare` 的 `existing` 命中按已保存 Entry 处理，draft 命中仍要求 `record save`。commit 前必须通过 active `show` 与 scoped `check`；旧 error 只记录为 pre-existing warning，新 error 或无法建立 baseline 时 fail closed。Research alerts 使用稳定 fingerprint 并保留 cleared history，区分 active blocker、historical unresolved、superseded retry 和 warning。官方六个 0.8.0 fixture 的 parser/contract tests 通过；Research public contract 现在同步 typed evidence review 与 action-bound run observation；仍没有 live AITP CLI subprocess conformance 证据。run observation 只记录外部 scheduler 观察，不提交、不轮询、不创建 campaign。 |

| 2026-08-28 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（AITP 外部事实未重验；Hakimi Web UI/cutover） | 本次未访问外部 AITP checkout，因此 HEAD、0.8.0、CLI/schema/gate 和 154 tests 原值不变，不形成新的 AITP 侧核验。Hakimi Web 已接通实验性 Research UI：TUI/Web `/research`；live Board 投影 scientific phase、latest progress、current action/run、effective next step、human gate 与 active alerts；line-first Manager 的 Science 区可 resolve decision、acknowledge alert、review evidence 和 observe run；stale refresh 与显式已有 AITP `entryId` checkpoint linking 保持 fail closed，Web 不写 AITP。`apps/kimi-web` production source cutover 已完成，schema v5 provenance 绑定 source、recipe files、实际 canonical Node/pnpm 与 bundle，v4 native receipt 再直接绑定 toolchain 和 binary hash。H5 仍仅部分集成，H6 仍 planned、unavailable。 |

2026-08-27 Hakimi Research Loop 收敛：模型正常行动路径改为 `BeginResearchAction` → 科研工作 → `ConcludeResearchAction`；后者由现有 Research service 在单一 transition 边界内记录 action completion 和科学 progress。`PlanResearchAction`、`CompleteResearchAction`、`SetResearchPhase` 保留为恢复/维护实现但不再进入 active tool overlay；没有新增 AITP schema、wire op、REST command 或自动 HPC 行为。相关 service/tool tests 通过；live AITP CLI subprocess conformance 仍 pending。

| 2026-08-28 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（done；Research contract/domain pack fusion） | Hakimi 将 Goal completion/continuation 通过通用 contribution seam 与 Research Loop 解耦，Research context 改为 Brief/Delta，committed AITP fact 通过 facade 隔离；抽出协议无关的 Research 类型、evidence packet 和 transition authority，并新增可选 `theory-physics` domain pack。Research 是长生命周期科研上下文，Plan 是可嵌套的短生命周期 overlay；二者可以同时 active，Plan 不会退出或重置 Research。该 pack 只提供理论物理行动路由、推导/数值证据检查和 science-first reporting，不改变 AITP schema，不提供文献库、scheduler observer 或 method distillation。
| 2026-08-29 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | M1e（live CLI smoke verified） | 使用真实 managed AITP 0.8.0 CLI 在一次性 scratch store 验证 `enter`/`check`（scoped，check exit 1 findings）、`record prepare`/`record save`、`note prepare`/`note save`、`show`/`list`、重复 idempotency prepare（`existing`）和最终 clean check。此前 fixture-only 证据已补充为实际 CLI 子进程证据；完整跨平台及异常矩阵仍 pending。 |
| 2026-08-29 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Hakimi capability graduation（未修改 AITP 仓库） | Codex OAuth 与 AITP Research Mode 的入口默认可发现；旧两个 experimental env/config keys 与 master flag 对它们均 inert no-op，`--enable-experimental` 为 deprecated no-op。新 session 初始为 inactive；hydration 保留已持久化的 mode，inactive hydration/GET/SDK read 不做 AITP I/O；持久化为 active 的 session 在 cold restore 后仍保持 active，并重新 probe/maintenance。显式 `enter_mode`/`/research on` 才 probe；active、admitted 的 Goal continuation turn 在 Research state 发生变化后的 turn end 也执行只读 maintenance。Wire/REST/SDK/AITP transport schema 与 checkpoint barrier 未改变。H0–H4 为 implemented-in-code，H5 仍 partial，H6b method distillation 为 planned/unavailable，完整跨平台及异常矩阵 conformance-pending。 |
| 2026-08-31 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Unified Research Mode S1（未修改 AITP 仓库） | Hakimi Research turn admission 现在区分 runtime-only `interactive_research` 与 `autonomous_research` lease。active、ready、loop-running 的 typed main-agent user prompt 进入一次 interactive Research Loop iteration 并获得 context injection；只有现有 continuation participants 放行后由通用 Goal engine 生成的 typed continuation 才获得 autonomous lease。interactive lease 不 enqueue，Goal 仍是唯一 continuation owner；system/cron/subagent/unclassified、inactive/probing/paused/degraded turn abstain。两类 admitted turn 的 loopCount 与 state-change 后只读 maintenance 均有回归测试；未新增 wire/REST/SDK/AITP schema，未写 AITP ledger，H5/H6b 状态不变。 |
| 2026-09-01 | `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Unified Research Mode S5（Hakimi implementation closed；strong gate blocked；未修改 AITP 仓库） | Binding 新增 server-owned `confirmationId`，clear 以 non-rewinding public revision + exact identity 防 ABA；REST/WS/SDK/klient/TUI/Web 严格同步。checkpoint post-save `show` 只接受 captured Topic 与唯一 captured workstream，mismatch 不 adoption 并 degraded/recovery。但 AITP 0.8 `record save` 没有 expected-Topic/exact-workstream precondition，无法阻止 edited draft 先生成额外 scope 的 canonical Entry；需要新 reviewed AITP contract，按 hard stop 记为 `planned / unavailable`，S5 strong gate 未通过，S6 未启动。 |
| 2026-09-01 | working tree after `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Unified Research Mode S5.1（strong gate passed；S6 未启动） | 用户 reviewed 的 AITP 0.9.0 slice 新增 adapter-contract 0.2 与 paired atomic `record save --expected-topic --exact-workstream`。Hakimi 兼容 0.1/0.2，但 checkpoint save 要求 0.2 并从 captured binding 自动传参；AITP 在写锁内 compare current Topic、exact singleton membership、validation、retry 与 canonical write。mismatch 零 canonical write；同 draft retry 保持 `already_saved`；post-save barrier 不删。AITP full suite 181 pass；Hakimi S5.1/Research/Goal 定向 659 pass，Agent Core 全套 5,823 pass、1 skip，TUI 459 pass、Web 444 pass，Research SDK/WS 16 pass；完整证据见对方 `docs/hakimi-s5-1-stage-notes.md`。S6 未启动。 |
| 2026-09-01 | working tree after `2b2636fd6c2a6c84268e44edae2b66f7a7215e3c` | Unified Research Mode S6（complete；release gate closed） | `ConcludeResearchAction` 要求一次显式 `no_durable_delta | durable_delta` assessment，并原子记录 action conclusion、唯一 progress boundary 与最多一个 typed pending `commitCandidate`。no-delta 不安排 S6 AITP write/review；positive candidate 锁定 Entry kind/authority/provenance/rationale 后复用 prepare/fill/atomic-save/show/scoped-check/commit barrier。相同 retry 幂等，不同 retry、重复 progress、provenance mismatch、pending durability 下的 Question/Line/Research Plan/Goal formal closure fail closed；REST/WS/Node SDK/klient/TUI/Web 同步。Agent Core 5,835 pass/1 skip，TUI 3,150 pass/2 skip，Web 913 pass，protocol 576 pass，AITP unchanged suite 181 pass，相关 typecheck/Web-assets check 通过；kap-server/klient/Node SDK 全套仍复现与 S6 无关的 prompt-abort、workspace-Skill discovery、MCP trust fixture 基线失败。用户明确授权 SDK major 与 CLI patch；`.changeset/assess-research-durability.md` 已生成且 `pnpm changeset status` 通过。S6 发布门关闭，S7 可启动。 |
| 2026-09-01 | working tree after `2b2636fd6c2a6c84268e44edae2b66f7a7215e3c` | Unified Research Mode S7（complete；S8 可启动） | 首次成功 commit 一个新 checkpoint 后，Agent-scope 无状态 handoff 精确加载 `aitp-research-protocol:distilling-methods`，在同一轮只把 touched Entry/checkpoint 交给外部 Skill 做一次 bounded review；duplicate commit 与 Skill 缺失/隐藏/model-disabled/加载失败均非阻塞 no-op。外部 Skill 独占 card 检索、pin/marker、trigger、basis/trial、revision 与 human gate 语义；无 public snapshot/schema、AITP runtime/CLI/contract、durable scheduler 或 H6b 变化。focused Skill/Research 401 pass；Agent Core 第二次串行全套 5,838 pass/1 skip（第一次仅有一个 unrelated `/tmp` cleanup `ENOTEMPTY`，isolated 12 pass 且全套重跑通过）；typecheck/build/import-boundary 通过；AITP unchanged suite 181 pass。`.changeset/distill-research-evidence.md` 记录 CLI patch，`changeset status` 通过。 |
| 2026-09-01 | working tree after `2b2636fd6c2a6c84268e44edae2b66f7a7215e3c` | Unified Research Mode S8（complete；S9 可启动） | 公共 Research snapshot 新增可选 `hakimi/research-distillation-attention-0.1`，只把最新 exact committed checkpoint/Entry 的 handoff 投影为 `review_requested` 或 `handoff_unavailable`。receipt 由 Agent-scope handoff service 写入 monotonic non-checkpointed model；新 commit 隐藏旧 receipt，stale result 不覆盖。unavailable 进入通用 Board attention，requested 只显示 expanded provenance；字段不表示 trigger/card/trial/review completion/approval/publication，无 public mutation、retry、scheduler 或 H6b state。REST/WS/Node SDK/klient/TUI/Web 与 stale-response guard 同步。Agent Core 第二次串行全套 5,839 pass/1 skip，TUI 3,151 pass/2 skip，Web 914、protocol 577、AITP unchanged 181 全部通过；REST/WS 106、Research SDK 18 与 S8 schema/Board 定向门禁通过；typecheck、manifest、import boundary、Agent Core/SDK/CLI build、521-file Web asset reproducibility 与双方 diff check 通过。kap-server/klient/Node SDK 全套仍只复现既有 prompt-abort/workspace-Skill/MCP-trust 基线失败。S8 changeset 记录 CLI patch + SDK additive minor（累计 release 仍受已授权 SDK major 支配）。 |
| 2026-09-01 | Hakimi working tree after `2b2636fd6c2a6c84268e44edae2b66f7a7215e3c`; initialized GW/LibRPA AITP workspace | Unified Research Mode S9（complete；S10 active） | 用真实 ABACUS job-1097 本地 packet 做 bounded read-only revalidation：9/9 manifest files、313 rows、class counts 与 summary 一致、0 blockers；错误 source expectation exit 2 且 zero output，正确 run 创建 deterministic report，retry `unchanged`。AITP 新 workstream pre/post check 均 clean，scope 外保留 71 errors/201 warnings；0.9 atomic save 创建 `entry-a071eb42792548f685520d4492615a63`，retry `already_saved`。只记录一个 `method-observation: manifest-verified-union-gate`；无 card/trigger，Skill review no-op。real-derived fixture 在 protocol/REST/WS/Node SDK/klient/TUI/Web 保留 exact revision/binding/checkpoint/Entry/handoff；TUI Board 60/60，parity retry unchanged。无 ABACUS/MPI/remote 运行，无 AITP implementation repo runtime/schema/contract 变化。Board 尚不单独公开 scoped check 的 `outside_scope` counts，保持 `planned / unavailable`；combined physical contract 仍 `UNVALIDATED`。 |
| 2026-09-01 | Hakimi working tree after `2b2636fd6c2a6c84268e44edae2b66f7a7215e3c`; AITP HEAD `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Unified Research Mode S10（complete；finite Program closed） | 完整重跑：Agent Core 5,839 pass/1 skip、TUI 3,152 pass/2 skip、Web 914、protocol 577、AITP 181；七包 typecheck/build、1,288-file import boundary、14-test manifest、521-file Web assets、CLI/help/version、adapter contract、runtime budget 与双方 diff check 通过。kap-server/klient/Node SDK 全套只复现既有非 Research workspace-Skill、prompt-abort、MCP-trust/open-handle 基线失败，Research-only SDK RPC 13/13 通过；不把基线失败记 green。Program 六份 changeset 全为 CLI patch，SDK 聚合 major；全仓 CLI 因 committed baseline 其他 minor changeset 仍聚合 minor，未执行 version。S9 未覆盖 live crash/cold-resume，故不新增 recovery contract、不声称 exactly-once；H6b 与 Board `outside_scope` projection 保持 `planned / unavailable`。无 ABACUS/MPI/remote、无 AITP runtime/schema/contract/Skill/human-decision 变化。 |
| 2026-09-02 | Hakimi HEAD `6452a0934429b7110707a4582a722226437b1b78`; AITP working tree after `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Goal continuation observability O1（Hakimi-only；complete） | Generic Goal snapshot 新增 optional derived continuation state（`idle/deciding/enqueued/running/held/waiting`）；held 精确保留 participant owner/reason，并同步 REST/WS/Node SDK/klient/transcript/TUI/Web 与 Research snapshot compatibility projection。Goal lifecycle 仍可为 active，Board/Goal strip 显示 `active · continuation held`，不创建第二套 lifecycle 或 scheduler。Agent Core 5,845 pass/1 skip、TUI 单 worker 3,154 pass/2 skip、Web 921、protocol 577、transcript 117、AITP unchanged 181 通过；定向 kap/klient/SDK 与所有相关 typecheck、CLI production build、521-file Web asset reproducibility、changeset status、diff check 通过。Node SDK 全套仍只复现既有 workspace-Skill/MCP-trust fixture 基线失败，未记为 green。AITP checkout 只读，CLI/schema/adapter contract/fixtures/Skill/human-decision 未改；H6b 与 Board `outside_scope` projection 仍 `planned / unavailable`。 |
| 2026-09-03 | Hakimi working tree after `6452a0934429b7110707a4582a722226437b1b78`; AITP HEAD `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Research Board/state consistency O2（Hakimi-only；complete） | compact TUI/Web Board 收敛为 Project/Current cycle/Attention/Next；legacy `loopCount` 显示为 Research turns，raw phase 只映射到五个 display stage。健康 AITP/provenance 折叠；current Line 之外的 focus/action/alerts 不进入当前 attention。Line switch 对 live Action/Run、pending checkpoint、unresolved human gate与 non-idle phase fail closed，并先归档安全 cycle 的 focus/progress。reconciliation 只处理 deterministic Hakimi-local state；interactive Research 仍不要求 Goal，Goal 仍独占 autonomous continuation/lifecycle。无 public contract 或 AITP 变更。定向 Agent Core 397、Web 116、TUI 70；单 worker Agent Core 5,847 pass/1 skip、TUI 3,163 pass/2 skip、Web 930、AITP unchanged 181 全部通过。三侧 typecheck、1,288-file import boundary、Agent Core/CLI build、521-file Web asset reproducibility、官方六 fixture byte parity、CLI/help/version/adapter contract、changeset status 与双方 diff check 通过；`.changeset/scope-research-board-cycles.md` 记录 CLI patch，SDK 未新增 bump。全测试文件 type-aware lint 只复现 committed HEAD 已存在的 `String(result.output)` error；O2 生产文件为 0 lint error。H6b、公开 `outside_scope` Board projection、语义推断/自动 AITP 修复仍为 `planned / unavailable`。 |
| 2026-09-03 | Hakimi working tree after `6452a0934429b7110707a4582a722226437b1b78`; AITP HEAD `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Research replay/recovery O3（Hakimi-only；complete） | 新增真实 0.21 wire 形状派生的匿名单 Line 与双 Line replay fixtures：旧 Goal 缺少 optional continuation 时保持可读并显示 legacy unavailable；known states 保留、unknown future enum fail closed。current Line 独占 compact Question/Action/Run/gate/alert/Next/continuation attention，其他 Line 只保留 expanded/management 可见性。reconcile 对唯一可确定的 stranded live Action/phase 结构幂等恢复，保留 Action 与 human resolution；live Action 阻止 Goal completion，recovered action hold autonomous continuation，并把下一次 interactive Research turn 导向 evidence-first completion/abandonment，不自动作科学终态判断，也不问纯 bookkeeping 问题。无 public contract 或 AITP 变更。单 worker Agent Core 5,850 pass/1 skip、TUI 3,166 pass/2 skip、Web 934、protocol 577、transcript 117、AITP unchanged 181 全部通过；REST/WS 90、Node SDK 100、klient Research facade 50 定向通过。klient 全套 137 pass/24 skip，仍只复现既有 memory/IPC workspace-Skill fixture 两项失败，未记 green。八个相关包 typecheck、1,288-file import boundary、生产文件 0 lint error、双语 docs build、CLI production build、521-file Web asset provenance、CLI/help/version、changeset status 与双方 diff check 通过；`.changeset/scope-research-board-cycles.md` 保持 CLI patch，SDK 未新增 bump。科学 completion/abandonment 推断、AITP semantic repair、H6b 与公开 `outside_scope` Board projection 仍为 `planned / unavailable`。 |
| 2026-09-05 | Hakimi isolated worktree at `015763451d8b6ec01a9954ae2239e76005952581` (latest `origin/main`); AITP HEAD `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290` | Research execution ownership O4（Hakimi-only；implementation complete，default） | 三个真实 Research Mode 导出暴露的 `BeginResearchAction` 被拒后仍执行 Web/Read/Shell 等无归属工作路径，现由统一 Tool Executor before-execute 层在 active Research Mode 中默认 fail closed：admitted Research turn 的科研工具需要 fresh、in-progress Action 与显式 capability；unknown plugin/MCP 要求 exact tool grant；Begin 与工作工具不得同 batch。checkpoint draft 和 post-commit Note 各用窄 lease，canonical `.aitp/topic` 禁止直接 Read/Edit/Write。无 receipt/Entry/history 且 captured revision/binding/Program 可证明过期的 checkpoint 可幂等自动或显式 discard；unresolved human gate 的 phase drift 只作机械恢复；alert clear 不再重复写。`collaborative|dreaming` 是 Research planning policy，`auto` 只处理工具审批与普通问题，不能回答 Research/AITP human decision；Goal 仍独占跨 turn continuation。compact Board 继续只显示 Project/Current cycle/Attention/Next。真实导出组合回归覆盖 revision 4→7、`state_updated`、unresolved gate、completed Action，恢复后无新 Action 的 WebSearch 被硬拒绝。裸 `/research` 在 TUI/Web 中按 authoritative snapshot 直接切换 mode；`on`/`off` 保留为显式兼容形式。AITP runtime/CLI/schema/contract/ledger 未改。executor policy 不是 OS-level sandbox，获准 shell 仍很宽；H6b exactly-once distillation、并行 loops、自动科学决定和自动 Goal/Program 或 Line/workstream 归属仍 `planned / unavailable`。 |

每次核对后：追加一行，并更新 `compatibility-matrix.md` 中变化的行与双语 README。

## 开发前核对 checklist（每次 AITP 相关开发必做）

1. [ ] AITP 开发仓库 `git rev-parse HEAD` 与 `git status --short`（注意既有未跟踪 `ref/`、`uv.lock`，勿触碰）。
2. [ ] `aitp --help` 确认实际 CLI 面；对照 `docs/roadmap.md` stage 状态表。
3. [ ] 核对 AITP `docs/hakimi/`（对方交接文档）是否有新决策或矩阵变化。
4. [ ] 确认涉及的命令/schema 是否 available；blocked 的一律不调用、不写 available。
5. [ ] 核对官方 fixtures 位置（`tests/ledger/fixtures/`、`suite/`）；需要 golden 时优先用官方，不用 planning examples。
6. [ ] 确认 FROZEN v6 约束是否仍生效（M0.6 scored run 前不得改冻结输入）。
7. [ ] 改动影响兼容性时：同 change 更新 `docs/aitp/`（本目录）+ 双语 README。

## 双方待办

### AITP 侧（状态见 AITP `docs/hakimi/`）

- [x] M0.6：以 2026-08-10 缩小声明关闭（原始 bootstrap/评分证据未测量，deferred，不计入）。
- [x] M1a：`aitp/enter-0.2`、`aitp/list-0.1`、`aitp/show-0.1` + golden fixtures；deterministic gate passed。
- [x] M1b-R1：`check` v0.1-only + compact `enter` 文本；deterministic gate passed；B/C–E/Followup 2/6 deferred。
- [x] M1c：workstreams（作用域投影 + repeatable prepare flag）；deterministic gate passed（107 tests）。
- [x] M1d：scoped `check` workstream health（`check-report-0.2`，单次 `--workstream`，admitted in-scope 计数、`by_code`/`outside_scope`）；deterministic gate passed。
- [x] M1e：evidence lifecycle + reviewed backfill（`sha256-once:` pin、`backfill` 命令、check-policy.json）；deterministic gate passed（154 tests，committed HEAD 0.8.0）。
- [ ] M2–M4：各自 natural-demand 证据未出现，blocked 设计选项；`lineage`/`lite-entry-0.2`/`run-pointer` 保持 deferred。
- [x] AITP 0.8 Skill-only amendment：`method-observation` marker、两步 human decision（approval + publication）、platform tool/card/Skill 三层。**已 commit**（HEAD `eae1bce5…`），不改 CLI/schema/transport。
- [ ] （已决策）prepare/save 不版本化；如未来硬化需 documented spec revision/addendum。

### Hakimi 侧

- [x] **H0（implemented-in-code）**：`features/aitpResearch/` adapter 骨架——launcher（argv-only、Python ≥ 3.11 探测）、version-0 严格 shape 校验、capability 探测、`enter` lifecycle、prepare→save 流程、`not_initialized` 优雅降级、tree-hash 零写入测试。
- [x] D 轨 research loop foundation（Research Frame / Question Board / bounded checkpoints）——不依赖 AITP，可并行。
- [x] H1（M1a gate 已通过，implemented-in-code）：feature-detect 并消费 `enter-0.2`/`list-0.1`/`show-0.1`；当前状态维护不是 closeout-first，也不是 session-end closeout。官方 0.8.0 六个 golden fixtures 仅用于本地 parser/contract 兼容测试，不启动 live CLI subprocess。
- [x] H2（M1b-R1 gate 已通过，implemented-in-code）：只整合 R1 实际发布的 `check`（`check-report-0.1`，exit 0/1 报告、exit 2 错误包）；`lite-entry-0.2` 关系、typed resolution、派生 `used_by`、pointer bundle 均未发布（deferred），不得安排。
- [x] H3（M1c gate 已通过，implemented-in-code）：整合 scoped contracts——仅传入单次 `--workstream <slug>` 时 feature-detect `enter-0.3`/`list-0.2`（additive `workstream` key、严格 exact membership、relation 先全局计算）；无 flag 时保持旧 schema。
- [x] H4（M1d gate 已通过，implemented-in-code）：整合 scoped `check`——仅传入单次 `--workstream <slug>` 时 feature-detect `check-report-0.2`（admitted in-scope 计数、`by_code`/`outside_scope`，四行文本仅人阅）；无 flag 时 `check-report-0.1` 字节不变。
- H5（M1e gate 已通过，部分集成）：AITP upstream 已 shipped `backfill-0.1`、`sha256-once:` 与 policy 语义；Hakimi adapter 只把 check finding code 作为 opaque string 投影，不暴露、不调用、不解析 backfill 成功 envelope，也不实现这些语义；完整 conformance 仍 pending。
- [x] Web UI：`/research`、live Board、line-first 表单 Manager、`probing`/`degraded`、stale refresh 与显式已有 AITP `entryId` checkpoint linking 已接通；Web 不调用 AITP write CLI，也不写 canonical files。
- [ ] H6b（planned，unavailable）：native method-distillation orchestration——Session-scope coordinator、candidate/proposal lifecycle、human question + decision write、crash/resume。前置：H0–H5 全部落地 + reviewed adapter-contract extension。详见 [`method-distillation-orchestration.md`](method-distillation-orchestration.md)。

## 解阻链

`M0.6（缩小声明关闭）` → `M1a gate（passed）` → `M1b-R1 gate（passed）` → `M1c gate（passed）` → `M1d gate（passed）` → `M1e gate（passed）` → `M2–M4（自然需求证据，blocked）`。

- H0：**implemented-in-code**，无 AITP 前置（无需 AITP change）。
- H1：M1a gate 已通过，feature-detect `enter-0.2`/`list-0.1`/`show-0.1`。**implemented-in-code**。
- H2：M1b-R1 gate 已通过，只可消费 `check-report-0.1`；其余 deferred。**implemented-in-code**。
- H3：M1c gate 已通过，可整合 scoped contracts（单次 `--workstream`）。**implemented-in-code**。
- H4：M1d gate 已通过，可整合 scoped `check`（`check-report-0.2`）。**implemented-in-code**。
- H5：M1e gate 已通过，但 Hakimi 仅部分集成：只投影 check finding code 的 opaque string，不暴露、不调用、不解析 `backfill-0.1` 成功 envelope，也不实现 `sha256-once:`/policy 语义；完整 conformance 仍 pending。
- H6b：**planned，unavailable**。前置：H0–H5 全部落地 + reviewed
  adapter-contract extension 冻结 marker discovery/exact-card trial/
  decision receipt semantics。
- 正式 Hakimi contract：等 M4 后 AITP versioned JSON + extended golden fixtures。

2026-08-30 audit：重新核对 AITP HEAD `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290`；其工作树已有用户未提交变化，本次在其基础上最小同步并完整保留。双方 handoff 已按 Hakimi 2026-08-29 状态对齐；未改 AITP CLI/schema/stage，M2–M4 仍为 blocked 设计选项，Method card 的 Skill/Note 层与 Hakimi/external adapter 的平台执行边界保持不变。

2026-08-30 Research/Goal continuation 修正：Board 和模型注入现在区分 AITP Topic `Research goal` 与当前 `Goal milestone`；`auto` 下的常规 scope 内 Research Action 不再创建独立 approval gate，旧 action-linked approval 在恢复时以 standing auto authorization 记录并继续，review/decision gate 不自动解决；默认 v2 引擎上的 `/goal resume` 直接唤醒 Goal driver，不再注入合成 User 消息，legacy rollback 保留原兼容路径。本次未改 AITP CLI/schema/stage/H5/H6b 状态。

2026-08-30 Goal↔AITP Program alignment：Hakimi 现在将 Hakimi Goal、仅由 AITP `enter` 观测的顶层 Research Goal/Program 与 Local Research Loop 明确区分；从不写 `TOPIC.md` 或 AITP Topic。用户可用 `/research align same_program_goal|goal_parent_of_program|goal_milestone_in_program|unrelated|clear` 创建或清除仅在 Hakimi 内 checkpointed 的显式 binding；该命令要求当前同时存在 Goal 和 observed Program，使用 snapshot optimistic concurrency，绝不写 AITP，也不从文本相似度推断。无 binding 为 `confirmation_required`；Goal、Topic 或 Program observation 变化会使 binding `stale`；只有 `unrelated` 是明确 `conflict`。active Research Mode 中这些状态阻止 Goal completion 和 automatic continuation，inactive Goal 不受影响。**本次只读复核外部 AITP checkout：HEAD 仍为 `eae1bce5eba367a5f6db6ba73ff0912dd3a5e290`，工作树已有用户未提交修改，当前 shell 中 `aitp --help` 不可用；未修改外部 checkout，也未更新对方 `docs/hakimi/` handoff，因此不形成新的 CLI/schema/stage/H5/H6b 兼容性声明。**

2026-08-31 Unified Research Mode S1：active、ready、running 的 typed main-agent user prompt 获得 transient `interactive_research` lease；现有 Goal continuation participants 放行后由 Goal engine 生成的 typed continuation 获得 `autonomous_research` lease。两者都只驱动一次 Research Loop iteration；interactive 不 enqueue，system/cron/subagent/unclassified/inactive/probing/paused/degraded abstain。

2026-08-31 Unified Research Mode S2：Research snapshot 新增可选 `hakimi/research-goal-0.1` 投影，并同步 REST、WebSocket、Node SDK、klient、TUI 和 Web。它与当前 generic Goal ID 一对一，投影 objective、completion criterion、当前 Program/Line/Question scope、non-goals、完整 budget、派生 stop conditions、Goal↔Program relation、human gates、persistence guards 和 Research revision；不创建第二个 Goal model 或 scheduler。当前 generic Goal 没有结构化 non-goals/用户 stop-condition 输入，因此 `nonGoals` 诚实为空，stop conditions 仅由 budget、loop、mode、alignment、checkpoint、human gate 和 Goal lifecycle 派生。active Research Mode 在 probing/degraded、pending checkpoint、未解决 gate 或非 aligned Program relation 下同时阻止 automatic continuation 与 completion；interactive Research 仍可无 Goal 工作。AITP checkout 的 protected dirty handoff 未修改，AITP CLI/schema/contract/Skill/H5/H6b 状态不变。

2026-08-31 Unified Research Mode S3：Hakimi 新增 additive、checkpointed `hakimi/research-plan-0.2` 多轮计划，包含 Goal/Program observed-revision binding、milestones、evidence requirements、decision points、assumptions、current milestone、stop/replan conditions 和 monotonic revision；旧 bounded-action `ResearchPlan` 不改义，并以 `actionPlan` alias 同时投影。非 trivial Research Action 必须捕获 active Research Plan milestone 与 approved local Plan revision；simple reversible one-step action 仍有 immutable minimal-plan binding。Plan、Goal、Program、Line、Question 或 local Plan stale 时 start/conclude fail closed；服务自身不允许在 bound foreground action 活跃时修订、完成或丢弃计划。Plan completion 不关闭 Question、不写 AITP、不完成 Goal。生命周期命令和 Board/Manager 已同步 REST、WebSocket、Node SDK、klient、TUI、Web；AITP runtime/CLI/schema/contract/Skill/H5/H6b 不变，对方 protected dirty handoff 未修改。

2026-08-31 Unified Research Mode S4：Research snapshot 新增 checkpointed、Hakimi-local 的 `collaborative | dreaming` planning policy，默认 collaborative。collaborative 仅在完成或修订 Research Plan 所需的 consequential unknown 上路由到既有 `AskUserQuestion` broker；dismiss、空答或含糊答复不产生 Plan mutation。dreaming 只采用 reversible、low-cost、in-scope 默认项，并要求记录到 `ResearchPlanV2.assumptions`。两种策略都保持 expensive/irreversible action、tool permission、scientific convention ambiguity、Goal/scope change 和 AITP/human decision gate 为硬边界。策略通过 Research revision 乐观并发持久化，并同步注入、REST、WebSocket、Node SDK、klient、TUI、Web 和 Board；切换本身不写 AITP，也没有新增 question protocol、AITP CLI/schema/contract 或 H6b coordinator。AITP protected dirty handoff 仍未修改。

2026-08-31 Unified Research Mode S5：Hakimi 不再把 current Research Line slug 直接当作 AITP workstream。ready probe 后先用无作用域 `enter` 只观测 Topic，再从 Hakimi-local、revisioned binding 解决当前 Line 的 scope；binding 必须由 user 或 main agent 显式确认，绝不从 slug/text/path/ID 推断。`unbound`/`unavailable`/`stale`/`conflict` 可低风险本地探索，但 checkpoint propose/commit 与 Hakimi checkpoint adoption fail closed。checkpoint 捕获精确 binding，并在 prepare/show/scoped-check/commit 各步校验；换绑需先 clear，live action 或 pending checkpoint 期间不能变更。Line switch、空 scope、legacy unscoped records、`outside_scope`、stale/conflict、undo/cold restore 和六个官方 fixture 属于 Hakimi implementation coverage。REST、WebSocket、Node SDK、klient、TUI 和 Web 使用同一 snapshot；AITP 侧 protected dirty handoff 保持不动，未新增 CLI/schema/backfill/registry。该 implementation 已关闭，但 AITP 0.8 `record save` 缺少 expected Topic / exact workstream atomic precondition，edited draft 仍可先形成 canonical mismatched Entry；post-save `show` 只能拒绝 adoption。预防能力需要另行 reviewed 的 AITP contract change，当前 `planned / unavailable`，因此 S5 strong gate 仍 blocked、S6 未启动；H6b 继续 planned/unavailable。

2026-09-01 Unified Research Mode S5.1：上述 S5 blocked 记录保留为实现当时的历史边界；当前 reviewed AITP 0.9.0 adapter-contract 0.2 已补上 paired atomic expected-Topic/exact-workstream precondition。Hakimi checkpoint save 必须从 captured explicit binding 传入两值，未知 0.2 contract 时在 spawn 前 fail closed；AITP mismatch 保证 zero canonical write，same-draft retry 保持 idempotent。S5 strong gate 当前已通过，S6 仍未启动；H6b 继续 planned/unavailable。

2026-09-01 Unified Research Mode S6：正常 action 仍为 `BeginResearchAction` → 实际科研工作 → `ConcludeResearchAction`。Conclude 现在要求 main agent 明确判断 no-delta 或 durable-delta，并在单一 transition 中结束 action、写唯一 progress boundary；positive path 同时建立一个 checkpointed commit candidate，随后只使用既有 AITP 0.9.0 prepare/fill/atomic-save/show/scoped-check/commit barrier。无 durable delta 不安排 S6 ledger I/O 或 distillation；独立 session-boundary maintenance 不受影响。human assertion/decision 与 agent/tool/source verification 分开，重复 progress 与依赖 pending durability 的 formal closure 被 runtime 拒绝。AITP runtime/CLI/schema/adapter contract 未改变，H6b 继续 planned/unavailable。实现、核心门禁与发布版本裁决均已通过：用户明确授权 SDK major 与 CLI patch，`.changeset/assess-research-durability.md` 已生成且 `pnpm changeset status` 通过；S6 关闭，S7 启动。
