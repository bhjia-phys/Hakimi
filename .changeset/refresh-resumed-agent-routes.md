---
"@bhjia-phys/hakimi": patch
---

Route Agent, AgentSwarm, and Tower model selection through canonical `/preset` routes, refresh normal profile bindings on resume, and retain legacy `/secondary-model` data only as a deprecated compatibility fallback.
